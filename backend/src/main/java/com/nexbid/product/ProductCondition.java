package com.nexbid.product;

/**
 * EN: How used the item is (spec §7.3). Stated by the seller, shown on the lot.
 * VI: Mức độ đã qua sử dụng (spec §7.3). Người bán khai, hiển thị trên lô hàng.
 */
public enum ProductCondition {
    NEW,
    LIKE_NEW,
    GOOD,
    FAIR,
    USED
}
