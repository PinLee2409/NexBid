package com.nexbid.payment;

/**
 * EN: What the demo winner chooses (spec §17). Standing in for what a payment provider would report.
 * VI: Thứ người thắng chọn khi demo (spec §17). Đứng thay cho kết quả mà cổng thanh toán thật sẽ trả về.
 */
public enum PaymentOutcome {
    SUCCESS,
    FAILED
}
